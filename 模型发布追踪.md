# 模型发布追踪

最后更新：2026-06-25
参考文档：`/home/ifnodoraemon/myreport/AI三巨头博客追踪.md`

跟踪范围：近期高信号 AI 模型发布，优先关注 `OpenAI`、`Anthropic`、`Google`，以及和 `coding`、`agent`、`tool use`、`长上下文`、`多模态`、`成本分层` 相关的能力变化

## 目的

这份文件作为长期维护的模型发布记录，用于：

- 统一记录重要模型发布，而不是把信息散落在博客文档里
- 区分 `frontier model`、`高吞吐小模型`、`coding model`、`agent model`
- 追踪模型能力、价格定位、工具能力和适用场景的变化
- 为后续选型、评测和成本判断提供对照基线

## 相关页面

详见 `wiki/` 中的长期模型页与比较页：

- [wiki/models/gpt-5-4.md](/home/ifnodoraemon/myreport/wiki/models/gpt-5-4.md)
- [wiki/models/gemma-4.md](/home/ifnodoraemon/myreport/wiki/models/gemma-4.md)
- [wiki/comparisons/gpt-5-4-vs-gemma-4.md](/home/ifnodoraemon/myreport/wiki/comparisons/gpt-5-4-vs-gemma-4.md)

## 当前判断

当前最值得关注的高信号主题：

1. 大厂模型发布已经从“更强通用模型”转向“`frontier + 低成本 + agent-friendly + domain-specialized`”分层。
2. `coding`、`tool use`、`computer use`、`knowledge work`、`scientific workflow` 正在成为旗舰模型默认卖点。
3. 真正影响工程落地的，不只是 benchmark，而是 `价格 / 吞吐 / 上下文 / 工具调用 / 运行环境 / 访问门控 / 安全约束` 的组合。

## 跟踪表

| 公司 | 日期 | 模型 | 定位 | 核心变化 | 与我们的相关性 | 优先级 | 建议动作 | 来源 |
|---|---|---|---|---|---|---|---|---|
| OpenAI | 2026-04-23 | GPT-5.5 | Frontier / agentic coding / knowledge work / research | 接替 `GPT-5.4` 成为最新公开主线，强化 `agentic coding`、`computer use`、`knowledge work`、`scientific research` 和更严格的 cyber/bio safeguard | 如果我们要比较通用工作流 agent 能力，这个版本必须作为最新 baseline | P0 | 重点记录它在 `tool use`、`long-horizon task`、`科学工作流`、`安全门控` 上的外部信号 | https://openai.com/index/introducing-gpt-5-5/ |
| Anthropic | 2026-04-16 | Claude Opus 4.7 | Frontier / coding / long-running agents / vision | 强化复杂软件工程、长时任务、指令遵循、高分辨率视觉、文件系统 memory，并加入 `xhigh effort` 与 `task budgets` | 如果我们评估 coding agent 供应商，这个条目要作为 Claude 侧公开可用最新基线 | P0 | 和 OpenAI 同期旗舰模型做横向对比，尤其是 coding、长任务、预算控制和 agent 运行方式 | https://www.anthropic.com/news/claude-opus-4-7 |
| Google | 2026-02-12 | Gemini 3 Deep Think | Frontier reasoning | 强调 `science`、`research`、`engineering` 场景的高难推理能力，是典型的 frontier reasoning 发布 | 如果我们更重视复杂推理和研究任务，这个方向值得优先观察 | P0 | 跟踪其 benchmark 叙事是否逐步转成产品和 API 叙事 | https://blog.google/innovation-and-ai/models-and-research/gemini-models/gemini-3-deep-think/ |
| Google | 2026-03-03 | Gemini 3.1 Flash-Lite | 高吞吐 / 低成本 / scale | 说明 Google 正在强化“足够聪明但极致便宜”的模型分层能力 | 对成本敏感的分类、检索辅助、轻量 agent 非常相关 | P0 | 建议把它加入后续小模型性价比对照表 | https://blog.google/innovation-and-ai/models-and-research/gemini-models/gemini-3-1-flash-lite/ |

## 当前优先级

### P0

- 跟踪 `frontier model` 的工具能力和长任务能力
- 跟踪 `低成本小模型` 的吞吐、价格和替代性
- 建立统一对照维度：`context`、`tool use`、`coding`、`multimodal`、`price`

### P1

- 补齐更多厂商和开源模型，但只保留高信号发布
- 把博客信号映射到真实模型选型和评测项

## 建议记录字段

后续新增模型时，建议至少记录：

- 模型名
- 公司
- 日期
- 定位
- 核心能力变化
- 工具 / agent 能力
- 成本 / 吞吐信号
- 适合场景
- 不适合场景
- 官方来源

## 每周更新模板

每周更新时复制下面这段：

```md
## YYYY-MM-DD 当周

### 新增模型

- 模型：
  公司：
  定位：
  核心变化：
  为什么重要：
  建议动作：

### 状态变化

- 模型：
  之前判断：
  当前判断：
  变化原因：

### 横向比较

- 对比项：
  结论：
  对我们的影响：

### 备注

- 
```

## 来源说明

- 优先使用官方发布页、官方博客和官方 API 文档
- 如果同一模型有多个页面，优先保留最能说明定位与能力边界的主发布页

## 2026-04-06 当周补充

### 新增模型 / 补录

- 模型：`Claude Opus 4.6`
  公司：`Anthropic`
  定位：`frontier / coding / long-horizon agent`
  核心变化：强调更强 `coding`、更稳定的大代码库操作、`1M context window beta`，并在 `Terminal-Bench 2.0`、`Humanity's Last Exam` 上继续把最强卖点锚定为 agentic work。
  为什么重要：这是 Anthropic 在 `Opus` 线上把“深推理”进一步和“长时 agent 执行”绑定。
  建议动作：继续把它作为 `最强 agent/coding` 档位的对照基线。
  来源：https://www.anthropic.com/news/claude-opus-4-6

- 模型：`Claude Sonnet 4.6`
  公司：`Anthropic`
  定位：`cost-effective frontier / agent-default`
  核心变化：在更低成本档位上给出更强的 `coding`、`computer use`、`long-context reasoning`、`agent planning`，并公开强调 `1M context beta`。
  为什么重要：说明 Anthropic 正把强 agent 能力往更便宜、默认的主力模型下沉。
  建议动作：模型选型上不要再简单用 `Sonnet=便宜, Opus=强` 这种旧二分法。
  来源：https://www.anthropic.com/news/claude-sonnet-4-6

- 模型：`Gemma 4`
  公司：`Google`
  定位：`open model / advanced reasoning / agentic workflows`
  核心变化：直接把 `function calling`、`structured JSON output`、`system instructions` 和 `agentic workflows` 作为开放模型能力来卖。
  为什么重要：这是一条明确的“开放模型也要 agent-friendly”信号。
  建议动作：把 `Gemma 4` 加入开源 / 开放模型对照表，不要只用它做轻量本地模型参考。
  来源：https://blog.google/innovation-and-ai/technology/developers-tools/gemma-4/

- 模型：`Gemini 3.1 Flash Live`
  公司：`Google`
  定位：`real-time audio / live interaction`
  核心变化：更低延迟、更自然的语音对话，目标是让实时语音交互成为默认表面。
  为什么重要：语音模型正在从“语音转文本配件”升级成独立模型线，这会影响 agent 入口设计。
  建议动作：若后续关注客服、助手或会议场景，应把 `live audio` 作为单独评测维度。
  来源：https://blog.google/innovation-and-ai/models-and-research/gemini-models/gemini-3-1-flash-live/

### 状态变化

- 模型分层：
  之前判断：大厂正在形成 `frontier + 低成本 + agent-friendly` 三层结构。
  当前判断：这套结构进一步清晰，但 `agent-friendly` 已不只属于旗舰模型，也在往 `Sonnet` 和 `Gemma` 这类更可部署档位下沉。
  变化原因：Anthropic 与 Google 都开始在更低成本或更开放的模型线上显式卖 `agentic workflows`。

- OpenAI 状态：
  之前判断：`GPT-5.4` 是当前主线。
  当前判断：截至 `2026-04-06`，OpenAI 公开主发布页仍以 `GPT-5.4` 为最新旗舰主线，本周没有同级别独立新模型发布页需要追加。
  变化原因：最新公开增量更多出现在 `spec / monitoring / product behavior`，而不是新模型代际。

## 2026-04-11 当周补充

### 新增模型 / 补录

- 模型：`Mythos preview`
  公司：`Anthropic`
  定位：`restricted preview / frontier security-oriented model`
  核心变化：截至 `2026-04-11`，Anthropic 官方站点已把 `Mythos preview` 列入模型入口；与 `2026-04-07` 的 `Project Glasswing` 同期出现，说明这是一条受限预览、非公开普发的最新模型线。
  为什么重要：这意味着 Anthropic 最新最强模型信号已经不止停留在 `Opus 4.6 / Sonnet 4.6`，而是进入了“高风险能力先受控部署”的阶段。
  建议动作：在模型追踪里单独标记 `restricted preview` 这一类，不要把它和公开 API / Claude.ai 普发模型混在一起。
  来源：https://www.anthropic.com/news ; https://www.anthropic.com/news?prog_id=13

- 模型：`GPT-5.4 mini`
  公司：`OpenAI`
  定位：`small model / coding / subagents`
  核心变化：OpenAI 公开把它写成更快、更便宜、适合 `coding assistants`、`computer use`、`subagents` 的执行层模型，并给出 `400k context`、`skills`、`computer use`、`web search` 等完整 agent 能力接口。
  为什么重要：这让 OpenAI 的模型分层第一次非常明确地服务于 `agent system design`，而不是只服务于聊天产品分层。
  建议动作：把 `GPT-5.4 mini` 作为高频执行、并行子任务和低成本 coding agent 的新基线。
  来源：https://openai.com/index/introducing-gpt-5-4-mini-and-nano/

- 模型：`GPT-5.4 nano`
  公司：`OpenAI`
  定位：`ultra-low-cost / extraction / ranking / simple coding subagents`
  核心变化：OpenAI 把它定位为最小、最便宜的 `GPT-5.4` 变体，适合分类、抽取、排序和较轻量的 coding subagents。
  为什么重要：这说明 OpenAI 已明确在 agent 体系内预留“极低成本执行层”。
  建议动作：后续在需要大规模并发或流水线拆分时，把 `nano` 纳入 cost baseline。
  来源：https://openai.com/index/introducing-gpt-5-4-mini-and-nano/

- 模型：`Gemini 3.1 Pro`
  公司：`Google`
  定位：`core reasoning / complex tasks / developer default`
  核心变化：Google 把 `3.1 Pro` 作为更普遍的复杂任务底座，并同步接入 `Gemini API`、`Vertex AI`、`Gemini app`、`NotebookLM`、`Gemini CLI` 和 `Antigravity`。
  为什么重要：这让 Google 的 developer stack 不再只有 `Deep Think` 这种高端推理档位，而有了更清楚的中枢模型。
  建议动作：在 Google 侧评测中，把 `3.1 Pro` 和 `Deep Think / Flash-Lite / Flash Live` 一起看作分层组合，而不是只盯一个旗舰点位。
  来源：https://blog.google/innovation-and-ai/models-and-research/gemini-models/gemini-3-1-pro/

### 状态变化

- OpenAI 模型分层：
  之前判断：`GPT-5.4` 是主线，其他增量更像产品能力补充。
  当前判断：OpenAI 已把 `GPT-5.4 -> mini -> nano` 明确做成 `planner -> executor -> cheap executor` 的可组合体系。
  变化原因：`mini / nano` 发布页直接把 `subagents`、`computer use`、`skills` 写进定位。

- Google 模型分层：
  之前判断：Google 的主线是 `Deep Think + Flash-Lite + Gemma 4`。
  当前判断：还要把 `Gemini 3.1 Pro` 视作开发者默认底座，否则会高估 Google 对专门档位的依赖。
  变化原因：Google 已把 `3.1 Pro` 分发到开发者、企业和消费者多个入口。

- Anthropic 最新模型状态：
  之前判断：最新公开模型发布是 `Claude Sonnet 4.6`。
  当前判断：如果只看“公开产品发布页”，仍然是 `Sonnet 4.6`；但如果看 `2026-04-11` 官方站点当前模型入口，最新更强的受限模型信号已经变成 `Mythos preview`。
  变化原因：Anthropic 已在官方站点的模型入口显示 `Mythos preview`，但没有用常规公开产品发布页大规模铺开。

## 2026-04-17 当周补充

### 新增模型 / 补录

- 模型：`Gemini 3.1 Flash TTS`
  公司：`Google`
  定位：`audio generation / expressive speech / developer distribution`
  核心变化：Google 推出新的文本转语音模型，强调 `granular audio tags`、`70+ languages`、更强可控性与更自然的表达，并同步进入 `Gemini API`、`Vertex AI`、`Google Vids`。
  为什么重要：这说明 Google 已把 `speech generation` 做成单独模型层，而不是从聊天模型附带导出语音。
  建议动作：如果后续涉及客服、配音、语音助手，应把 `TTS controllability` 和 `watermarking` 单列成评测项。
  来源：https://blog.google/innovation-and-ai/models-and-research/gemini-models/gemini-3-1-flash-tts/

- 模型：`Gemini Robotics-ER 1.6`
  公司：`Google`
  定位：`embodied reasoning / physical agent model`
  核心变化：这条模型线明确支持 `spatial reasoning`、`success detection`、`instrument reading`，并把 `agentic vision + code execution + external tools` 组合到 physical agent 工作流中。
  为什么重要：这不是普通多模态升级，而是把 agent 模型从数字界面延伸到实体执行环境。
  建议动作：在模型分层里单独保留 `robotics / embodied reasoning`，不要全部并到通用 Gemini 线。
  来源：https://deepmind.google/blog/gemini-robotics-er-1-6/

### 状态变化

- Google 模型分层：
  之前判断：Google 在 `Deep Think / Pro / Flash-Lite / Flash Live / Gemma 4` 上已经形成较完整组合。
  当前判断：还应再显式加入 `Flash TTS` 与 `Robotics-ER 1.6` 两条专门模型线，Google 的分层已扩展到 `语音生成 + embodied reasoning`。
  变化原因：`2026-04-14` 和 `2026-04-15` 两篇官方发布页都不是轻量产品更新，而是独立模型能力层补位。

- OpenAI / Anthropic 本周模型状态：
  之前判断：OpenAI 最近主增量在 runtime 和 Codex，Anthropic 最近主增量在 `Mythos preview` 与 managed agents。
  当前判断：截至 `2026-04-17`，这两个判断保持不变；本周没有看到同级别的公开新模型发布页需要改写主线。
  变化原因：两家新增主要集中在平台与工作流，而不是新模型代际。

## 2026-04-28 当周补充

### 新增模型 / 补录

- 模型：`GPT-5.5`
  公司：`OpenAI`
  定位：`frontier / agentic coding / knowledge work / scientific research`
  核心变化：GPT-5.5 已替代 `GPT-5.4` 成为 OpenAI 最新公开主线，强调更强 `agentic coding`、`computer use`、`knowledge work`、`scientific research`，并在 Codex 中提供 `400K context`，API 侧更新后提供 `1M context`。
  为什么重要：它把 `planner model` 的能力继续往真实长流程工作推进，尤其是工程、文档、科研和工具使用。
  建议动作：把 `GPT-5.5` 设为 OpenAI 侧最新 frontier baseline，同时记录它的 `cyber / bio` 高风险能力门控。
  来源：https://openai.com/index/introducing-gpt-5-5/

- 模型：`Claude Opus 4.7`
  公司：`Anthropic`
  定位：`frontier / coding / long-running agents / vision`
  核心变化：Opus 4.7 已成为 Anthropic 最新公开可用 Opus 模型，重点提升复杂软件工程、长时任务、指令遵循、视觉分辨率、文件系统 memory，并加入 `xhigh effort`、`task budgets` 等运行控制。
  为什么重要：Anthropic 的前沿模型能力进一步和 agent harness、预算控制、review、auto mode 绑定。
  建议动作：把 `Opus 4.7` 设为 Claude 侧公开可用 frontier baseline；`Mythos preview` 仍单独标记为受限预览。
  来源：https://www.anthropic.com/news/claude-opus-4-7

- 模型：`GPT-Rosalind`
  公司：`OpenAI`
  定位：`domain-specialized / life sciences / trusted access`
  核心变化：OpenAI 发布面向生命科学研究的专用模型系列预览，覆盖生物、药物发现、转化医学、科学工具调用，并通过 trusted access 限定可用范围。
  为什么重要：这说明 OpenAI 不只在通用 frontier 上迭代，也开始把高风险高价值领域做成专用模型线。
  建议动作：后续模型分层中新增 `domain-specialized frontier model`，不要把它和普通工具插件混在一起。
  来源：https://openai.com/index/introducing-gpt-rosalind/

### 状态变化

- OpenAI 最新模型状态：
  之前判断：`GPT-5.4` 是当前主线，`mini / nano` 是执行层补充。
  当前判断：`GPT-5.5` 已成为最新公开 frontier 主线，`GPT-5.4` 降为上一代基线；同时 `GPT-Rosalind` 显示 OpenAI 正在开出高门槛领域模型线。
  变化原因：OpenAI 在 `2026-04-23` 发布 GPT-5.5，并在 `2026-04-16` 发布 GPT-Rosalind 研究预览。

- Anthropic 最新模型状态：
  之前判断：`Mythos preview` 是更强受限模型信号，公开可用主线仍偏 `Opus / Sonnet 4.6`。
  当前判断：`Claude Opus 4.7` 已成为公开可用最新 Opus；`Mythos preview` 仍代表更高风险能力的 gated deployment 路线。
  变化原因：Opus 4.7 已在 Claude 产品、API、Bedrock、Vertex AI、Microsoft Foundry 可用。

- Google 最新模型状态：
  之前判断：Google 本月新增重点是 `Flash TTS` 与 `Robotics-ER 1.6`。
  当前判断：截至 `2026-04-28`，本周没有看到同级别独立新 Gemini 模型发布；Google 主要增量转向 `Gemini Enterprise Agent Platform` 与训练基础设施。
  变化原因：Cloud Next 26 的重点在平台、runtime、governance、TPU 和 data cloud，而不是单个模型代际。

## 2026-05-07 当周补充

### 新增模型 / 补录

- 模型：`GPT-5.5 Instant`
  公司：`OpenAI`
  定位：`default ChatGPT model / low-latency general model / personalized assistant`
  核心变化：替代 `GPT-5.3 Instant` 成为 ChatGPT 默认模型，并在 API 中作为 `chat-latest` 暴露；重点改善事实性、图像理解、STEM、web search 决策、简洁性和个性化上下文使用。
  为什么重要：这是 OpenAI 最高频入口模型的升级，不是 frontier thinking 模型，但会影响最多真实用户工作流。
  建议动作：把 `Instant` 档位单独建为 `default / high-volume / personalization` 基线，不和 `GPT-5.5 Thinking` 混写。
  来源日期：`2026-05-05`
  来源：https://openai.com/index/gpt-5-5-instant/ ; https://openai.com/index/gpt-5-5-instant-system-card/

- 模型：`Gemma 4 MTP drafters`
  公司：`Google`
  定位：`open model inference acceleration / speculative decoding`
  核心变化：Google 为 Gemma 4 family 发布 Multi-Token Prediction drafters，用专门 speculative decoding 架构提升推理速度，同时强调不损失输出质量和 reasoning logic。
  为什么重要：这不是新一代模型，但会改变开放模型的实际可用性，尤其是本地、端侧和高并发开发者场景。
  建议动作：在开放模型追踪中把 `base model capability` 和 `serving/inference add-on` 分开记录。
  来源日期：`2026-05-05`
  来源：https://blog.google/innovation-and-ai/technology/developers-tools/multi-token-prediction-gemma-4/

### 状态变化

- OpenAI 最新模型状态：
  之前判断：`GPT-5.5` 是 OpenAI 最新公开 frontier 主线。
  当前判断：判断保持不变，但 `GPT-5.5 Instant` 成为新的默认高频模型入口，应作为独立 baseline；系统卡显示默认模型也需要高能力类别安全分级。
  变化原因：OpenAI 在 `2026-05-05` 发布 Instant 更新和系统卡。

- Anthropic 最新模型状态：
  之前判断：`Claude Opus 4.7` 是公开可用最新 Opus，`Mythos preview` 是 gated deployment 路线。
  当前判断：本周未见同级别新 Claude 模型发布；新增重点是金融 agent 模板、API/Claude Code 限额和 SpaceX 算力合作。
  变化原因：Anthropic `2026-05-04` 至 `2026-05-06` 的新公开内容集中在行业产品与 capacity，而不是模型代际。

- Google 最新模型状态：
  之前判断：Google 最近模型新增重点是 `Flash TTS` 与 `Robotics-ER 1.6`。
  当前判断：本周没有同级别新 Gemini/Gemma 基座模型发布，但 `Gemma 4 MTP drafters` 说明开放模型线正在进入推理效率优化阶段。
  变化原因：Google `2026-05-05` 发布的是 Gemma 4 推理加速组件，而不是新模型代际。

### 横向比较

- 对比项：`frontier model` vs `default model` vs `inference acceleration`
  结论：OpenAI 本周补的是默认入口模型，Anthropic 补的是可用性和行业模板，Google 补的是开放模型推理效率。
  对我们的影响：后续选型不能只按“最强模型”排序，还要按 `默认入口质量`、`API/限额可用性`、`推理延迟` 三条线分别记录。

## 2026-05-14 当周补充

### 新增模型 / 补录

- 模型：`GPT-5.5-Cyber`
  公司：`OpenAI`
  定位：`trusted-access cyber model / domain-specialized frontier`
  核心变化：OpenAI 以 trusted access 方式开放面向网络安全研究和防御的 `GPT-5.5-Cyber`，强调高风险能力门控、合格使用者准入和安全评估。
  为什么重要：这是 `GPT-Rosalind` 之后又一条高价值高风险领域专用模型线，说明 OpenAI 正把 frontier 能力拆到受控垂直模型。
  建议动作：模型分层新增 `trusted-access domain model`，并单独记录准入条件、能力边界和安全卡。
  来源日期：`2026-05-07`
  来源：https://openai.com/index/gpt-5-5-with-trusted-access-for-cyber/

- 模型：`GPT-5.5 Instant` `能力面补录`
  公司：`OpenAI`
  定位：`default model / multimodal consumer surface`
  核心变化：ChatGPT release notes 于 `2026-05-12` 记录免费用户可用更多图像生成次数，背后仍是 `GPT-5.5 Instant` 作为高频默认入口模型承担多模态体验扩展。
  为什么重要：这不是新模型代际，但说明默认模型的产品面正在快速变宽，模型追踪需要记录 `capability surface expansion`。
  建议动作：在默认模型 baseline 中单独记录多模态额度、入口和产品约束。
  来源日期：`2026-05-12`
  来源：https://help.openai.com/en/articles/6825453-chatgpt-release-notes

### 状态变化

- OpenAI 最新模型状态：
  之前判断：`GPT-5.5` 是最新公开 frontier 主线，`GPT-5.5 Instant` 是默认高频入口。
  当前判断：还应补上 `GPT-5.5-Cyber` 作为受控垂直模型线，OpenAI 的模型分层已形成 `frontier / default / small executor / trusted-access domain` 多层组合。
  变化原因：OpenAI 在 `2026-05-07` 发布 cyber trusted access 页面，且本周没有看到同级别新通用模型代际替换 `GPT-5.5`。

- Anthropic 最新模型状态：
  之前判断：`Claude Opus 4.7` 是公开可用最新 Opus，`Mythos preview` 是 gated deployment 路线。
  当前判断：本周未见同级别新 Claude 模型发布；新增重点是 `Claude for Small Business` 的产品包装，而非模型代际。
  变化原因：Anthropic `2026-05-13` 新发布集中在小企业团队工作流。

- Google 最新模型状态：
  之前判断：Google 最近模型新增重点是 `Flash TTS`、`Robotics-ER 1.6` 与 Gemma 4 推理效率。
  当前判断：本周未见同级别新 Gemini/Gemma 基座模型发布；新增重点是 Gemini 进入 Android/Chrome 产品入口。
  变化原因：Google `2026-05-12` 的 `Gemini Intelligence` 与 `Chrome Auto Browse` 是模型能力分发表面，而不是新基座模型发布。

### 横向比较

- 对比项：`new model generation` vs `model surface expansion`
  结论：本周没有看到三家同步发布新通用基座模型；更重要的变化是 OpenAI 补受控领域模型，Google 扩 Gemini 入口，Anthropic 扩产品包装。
  对我们的影响：后续模型追踪不应只记“新模型名”，也要区分模型能力是否通过 trusted access、默认入口、OS/browser surface 或行业包被重新分发。

## 2026-05-21 当周补充

### 新增模型 / 补录

- 模型：`Gemini 3.5 Flash`
  公司：`Google`
  定位：`frontier-speed / agentic coding / long-horizon workflow`
  核心变化：Google 发布 Gemini 3.5 系列首个模型 `3.5 Flash`，强调 `frontier intelligence with action`、agentic coding、Terminal-Bench 2.1、GDPval-AA、MCP Atlas、multimodal understanding，以及与 Antigravity harness 和 Managed Agents 的联动。
  为什么重要：这是本周最重要的新模型发布，Google 把 Flash 档位直接推成 agent/coding 生产力主力，而不是只做低成本小模型。
  建议动作：把 `Gemini 3.5 Flash` 设为 Google 侧最新公开 agentic model baseline，并和 `GPT-5.5`、`Claude Opus 4.7` 做同维度对比。
  来源日期：`2026-05-19`
  来源：https://blog.google/innovation-and-ai/models-and-research/gemini-models/gemini-3-5/

- 模型：`Gemini 3.5 Pro`
  公司：`Google`
  定位：`frontier / pending rollout`
  核心变化：Google 在 Gemini 3.5 发布页中说明 `3.5 Pro` 已在内部使用，计划下月推出。
  为什么重要：这不是本周可用模型，但会成为后续 Google frontier baseline 变化的直接预告。
  建议动作：标记为 `预告 / 待发布`，下周继续确认是否开放 preview 或 API。
  来源日期：`2026-05-19`
  来源：https://blog.google/innovation-and-ai/models-and-research/gemini-models/gemini-3-5/

- 模型：`Gemini Omni Flash`
  公司：`Google`
  定位：`multimodal generation / video creation and editing`
  核心变化：Google 发布 Gemini Omni 家族首个模型 `Omni Flash`，支持从文本、图像、视频、音频等输入生成和对话式编辑视频，并通过 Gemini app、Google Flow、YouTube Shorts/Create 分发，后续将提供 API。
  为什么重要：这把 Gemini 多模态线推向视频级世界理解、可编辑生成和内容 provenance，是 Google 今年多模态产品化的核心新基线。
  建议动作：模型追踪新增 `video editing`、`input modality mixing`、`SynthID / verification`、`API availability` 字段。
  来源日期：`2026-05-19`
  来源：https://blog.google/innovation-and-ai/models-and-research/gemini-models/gemini-omni/

- 模型：`GPT-5.5 / Codex enterprise deployment`
  公司：`OpenAI`
  定位：`existing frontier/coding model surface expansion`
  核心变化：本周未见 OpenAI 新通用模型代际发布；新增重点是 Codex 与 Dell 合作进入 hybrid/on-prem 企业环境。
  为什么重要：这不是新模型，但会改变 Codex/GPT-5.5 系模型在企业生产环境中的可用边界和上下文接入方式。
  建议动作：模型追踪中保留为 `deployment surface expansion`，不误记为新模型。
  来源日期：`2026-05-18`
  来源：https://openai.com/index/dell-codex-enterprise-partnership/

### 状态变化

- OpenAI 最新模型状态：
  之前判断：`GPT-5.5` 是最新公开 frontier 主线，`GPT-5.5 Instant` 是默认高频入口，`GPT-5.5-Cyber` 是受控垂直模型线。
  当前判断：本周没有看到同级别新 OpenAI 通用模型发布；新增变化是 Codex 企业部署面扩大到 Dell hybrid/on-prem 环境。
  变化原因：OpenAI `2026-05-18` 公开内容是企业部署合作，而不是模型代际发布。

- Anthropic 最新模型状态：
  之前判断：`Claude Opus 4.7` 是公开可用最新 Opus，`Mythos preview` 是 gated deployment 路线。
  当前判断：本周没有看到新 Claude 模型代际；Anthropic 的新增重点是 Stainless 收购和 KPMG 企业采用，属于连接/采用层。
  变化原因：Anthropic `2026-05-18` 至 `2026-05-19` 新公开内容集中在 SDK/MCP tooling 与专业服务企业部署。

- Google 最新模型状态：
  之前判断：Google 最近没有同级别新 Gemini/Gemma 基座模型发布，重点在产品入口扩展。
  当前判断：`Gemini 3.5 Flash` 已成为 Google 侧最新公开 agentic model baseline，`Gemini Omni Flash` 成为最新多模态视频生成/编辑基线，`Gemini 3.5 Pro` 标记为内部使用、待下月推出。
  变化原因：Google I/O 2026 于 `2026-05-19` 集中发布 Gemini 3.5 与 Gemini Omni。

### 横向比较

- 对比项：`new model generation` vs `deployment/connectivity`
  结论：本周 Google 明确更新模型基线；OpenAI 和 Anthropic 没有同级别新通用模型发布，分别在企业部署和连接工具链上推进。
  对我们的影响：选型时要分开看 `模型能力更新`、`部署位置`、`connector/tooling`，否则会把平台能力扩展误判为模型代际提升。

## 2026-06-18 当周补充
## 2026-06-22 综合补充 (涵盖 5.22 - 06.22)
### 新增模型 / 补录

- 模型：`Claude Opus 4.8`
  公司：`Anthropic`
  定位：`frontier / complex agents / coding`
  核心变化：Anthropic 发布 `Claude Opus 4.8`，将其作为复杂 agent、coding 和长流程工作的最新高端模型，并在 TAU3 telecom、airline、retail 等任务中给出模型表现。
  为什么重要：Claude 侧最新公开 baseline 已从 Opus 4.7 切换到 Opus 4.8，且可通过 Claude、API、Amazon Bedrock、Google Cloud Vertex AI 和 Microsoft Foundry 使用。
  建议动作：把 Claude 对比表中的 `latest Opus` 更新为 `4.8`，同时把供应商自报 benchmark 标记为 vendor-reported。
  来源日期：`2026-06-10`
  来源：https://www.anthropic.com/news/claude-opus-4-8

- 模型：`Claude Fable 5 / Claude Mythos 5`
  公司：`Anthropic`
  定位：`cost-effective everyday model / highest-intelligence model`
  核心变化：Anthropic 于 `2026-06-05` 发布 Fable 5 与 Mythos 5，其中 Fable 5 定位为更便宜的日常模型，Mythos 5 定位为高智能、长程推理、困难 coding 与科学推理；随后于 `2026-06-17` 暂停访问以修复 release harness 技术问题，并于 `2026-06-18` 澄清访问限制与出口管制。
  为什么重要：这是一组重要模型发布，但当前可用性存在明确 caveat；模型追踪必须同时记录 release、pause 和 access clarification。
  建议动作：标记为 `发布但访问暂停/受限确认中`，恢复访问前不作为稳定生产 baseline。
  来源日期：`2026-06-05`、`2026-06-17`、`2026-06-18`
  来源：https://www.anthropic.com/news/claude-fable-5-mythos-5 ; https://www.anthropic.com/news/fable-mythos-access ; https://www.anthropic.com/news/fable-mythos-export-control

- 模型：`GPT-Rosalind`
  公司：`OpenAI`
  定位：`trusted researcher access / AI scientist / chemistry`
  核心变化：OpenAI 于 `2026-06-03` 为 GPT-Rosalind 增加文献综述、假设测试和 protocol design 能力，并于 `2026-06-16` 展示其与 Lila Sciences 合作的 AI chemist 场景。
  为什么重要：这不是通用聊天模型代际替换，而是 OpenAI 在科学工作流中的领域模型/能力面扩展。
  建议动作：模型追踪单独记录 `domain-specialized trusted access model`，避免把它误归入通用 frontier baseline。
  来源日期：`2026-06-03`、`2026-06-16`
  来源：https://openai.com/index/introducing-new-capabilities-to-gpt-rosalind/ ; https://openai.com/index/a-new-ai-chemist/

- 模型：`DiffusionGemma`
  公司：`Google DeepMind`
  定位：`diffusion-based LLM / Gemma 4 derivative / faster text generation`
  核心变化：Google DeepMind 发布 `DiffusionGemma`，将其描述为基于 Gemma 4 的首个 diffusion-based LLM，并强调预览版文本生成速度最高可达 4x。
  为什么重要：这是 Google 在主流 autoregressive LLM 之外的架构实验，值得观察是否会影响低延迟生成和本地推理路线。
  建议动作：不要和 Gemini frontier baseline 混淆；作为 `architecture experiment / open model derivative` 跟踪。
  来源日期：`2026-06-10`
  来源：https://deepmind.google/blog/diffusiongemma-4x-faster-text-generation/

- 模型 / 能力面：`Gemini 3.5 Live Translate`
  公司：`Google`
  定位：`real-time speech translation / multimodal product capability`
  核心变化：Google 将 Gemini 3.5 用于实时语音翻译，覆盖 23 种语言，并强调保留说话者意图、语气和情绪细节。
  为什么重要：这不是新基座模型，但会显著扩大 Gemini 3.5 的多模态产品面，尤其是实时语音场景。
  建议动作：模型表中对 Gemini 3.5 增加 `live speech translation` capability surface，而不是新建通用模型 baseline。
  来源日期：`2026-06-11`
  来源：https://blog.google/innovation-and-ai/models-and-research/gemini-models/gemini-live-3-5-translate/
  定位：`frontier / coding & dynamic workflows`
  核心变化：5月28日发布。在代码生成和智能体任务的长流程执行上进一步拉高 Opus 家族基线，并推出更经济的“快速模式”。
  为什么重要：巩固了 Anthropic 在长上下文与严谨研发场景下的主导地位。
  建议动作：更新 Opus 基线至 4.8。
  来源日期：`2026-05-28`

- 模型：`Claude Fable 5 & Mythos 5` (暂停状态)
  公司：`Anthropic`
  定位：`next-gen frontier / cyberdefense specialization`
  核心变化：6月9日发布，但于6月12日因美国出口管制及越狱漏洞风险全球暂停访问。Fable 5 后于6月18日恢复部分访问。
  为什么重要：这是首次由国家安全出口管制导致的旗舰级大模型紧急下线。
  建议动作：加入“出口管制与可用性中断”追踪标签。
  来源日期：`2026-06-09`

- 模型：`MAI-Thinking-1` & `MAI-Code-1-Flash`
  公司：`Microsoft`
  定位：`in-house reasoning & coding efficiency`
  核心变化：在 Build 2026 发布，强调极高的推理性价和 Token 成本优势，直接对标顶级代码模型。
  为什么重要：微软开始用自家模型替换部分 OpenAI 依赖。
  建议动作：引入 MAI 家族作为高效代码任务的 Alternative Baseline。
  来源日期：`2026-06-08`

- 模型：`GLM-5.2`
  公司：`Zhipu AI`
  定位：`open-weights / long context`
  核心变化：采用 MIT 协议开源，原生支持 100 万 Token 上下文。
  为什么重要：超长上下文的开源基线进一步提升。
  建议动作：在开源长文本场景测试中引入。
  来源日期：`2026-06-13`
### 状态变化

- OpenAI 最新模型状态：
  之前判断：`GPT-5.5` 是最新公开 frontier 主线，Codex 企业部署面扩大。
  当前判断：本周没有看到同级别通用 GPT 代际替换；新增重点是 `GPT-Rosalind` 的科学能力面和 AI chemist 展示。
  变化原因：OpenAI `2026-06-03` 至 `2026-06-17` 的公开内容集中在 AI for science、LifeSciBench 和公共利益部署模拟。

- Anthropic 最新模型状态：
  之前判断：`Claude Opus 4.7` 是公开可用最新 Opus，Fable/Mythos 属于 gated deployment 路线。
  当前判断：`Claude Opus 4.8` 成为最新 Opus baseline；`Claude Fable 5 / Mythos 5` 已发布但因 release harness 问题暂停访问，并有出口管制澄清。
  变化原因：Anthropic 在 `2026-06-05`、`2026-06-10`、`2026-06-17`、`2026-06-18` 连续更新 Claude 模型与可用性状态。

- Google 最新模型状态：
  之前判断：`Gemini 3.5 Flash` 是 Google 侧最新公开 agentic model baseline，`Gemini Omni Flash` 是多模态视频基线。
  当前判断：新增 `DiffusionGemma` 作为 diffusion LLM 架构实验，`Gemini 3.5 Live Translate` 作为实时语音产品能力扩展。
  变化原因：Google DeepMind `2026-06-10` 发布 DiffusionGemma，Google `2026-06-11` 发布 Gemini 3.5 Live Translate。

### 横向比较

- 对比项：`model baseline` vs `capability surface` vs `availability caveat`
  结论：本周真正切换 baseline 的是 Anthropic Opus 4.8；OpenAI 更偏 GPT-Rosalind 科学能力扩展，Google 更偏 DiffusionGemma 架构实验和 Gemini 3.5 产品能力面；Fable/Mythos 需要单独记录可用性 caveat。
  对我们的影响：后续模型选型不能只按发布日期排序，应同时看是否稳定可用、是否是通用 baseline、是否只是产品能力扩展。
  当前判断：GPT-5.5 依然是主力，但业内强烈预期 6月底推出 `GPT-5.6`（1.5M Context）。
  变化原因：无直接模型发布，但底层基础设施升级频发。

- Anthropic 最新模型状态：
  当前判断：由于 Fable/Mythos 的合规风波，当前可用的最稳定基石模型退回至 `Claude Opus 4.8`。

- Google 最新模型状态：
  当前判断：Gemini 3.5 Flash 与 Pro 继续渗透各个业务入口；推出了纯多模态无编码器模型 `Gemma 4 12B`。

## 2026-06-25 当周补充

### 新增模型 / 补录

- 模型：`GPT-5.5-Cyber` `full version`
  公司：`OpenAI`
  定位：`trusted-access cyber model / defensive patch workflow`
  核心变化：OpenAI 在 Daybreak 中宣布通过 continued limited release 向 trusted defenders 提供 `GPT-5.5-Cyber` full version，并称其在 CyberGym 达到 85.6%，高于 GPT-5.5 的 81.8%。
  为什么重要：OpenAI 的垂直高风险模型线继续产品化，但仍通过 trusted access 控制使用者和用途；模型价值和 Codex Security patch workflow 绑定，而不是单独开放通用 API。
  建议动作：模型追踪新增 `trusted-access defensive model` 字段，并记录准入、用途限制、评测指标和 patch workflow 依赖。
  来源日期：`2026-06-22`
  来源：https://openai.com/index/daybreak-securing-the-world/

- 模型：`Gemini 3.5 Flash with computer use`
  公司：`Google`
  定位：`frontier-speed / computer-use agent model`
  核心变化：Google 将 computer use 作为内建工具并入 `Gemini 3.5 Flash`，支持 browser、mobile、desktop 环境中的观察、推理和行动，并同步提供敏感操作确认与 indirect prompt injection stop 机制。
  为什么重要：这不是新的 Gemini 代际，但会改变 3.5 Flash 的 agent baseline：computer-use 从独立模型能力变成主力 Flash 模型的默认工具能力。
  建议动作：更新 Google baseline 时，将 `3.5 Flash` 标注为 `computer-use capable`，并把安全护栏与 API 可用性一起记录。
  来源日期：`2026-06-24`
  来源：https://blog.google/innovation-and-ai/models-and-research/gemini-models/introducing-computer-use-gemini-3-5-flash/

- 模型：`Claude Opus 4.8` `Claude Tag runtime surface`
  公司：`Anthropic`
  定位：`current Opus runtime / team collaboration surface`
  核心变化：Anthropic 发布 Claude Tag，并明确 Claude Tag works with `Opus 4.8`；本周没有看到新的 Claude 模型代际替换 Opus 4.8，但 Opus 4.8 的分发表面扩展到 Slack-native team agent。
  为什么重要：模型追踪不能只记录新模型名，也要记录同一模型进入新的高频协作入口，因为这会改变真实使用量、上下文形态和治理需求。
  建议动作：将 `Opus 4.8` 状态标为 `current stable Opus + team agent surface expanded`。
  来源日期：`2026-06-23`
  来源：https://www.anthropic.com/news/introducing-claude-tag

- 模型：`GPT-5.3-Codex-Spark` `Jalapeño workload signal`
  公司：`OpenAI`
  定位：`coding model serving workload / inference hardware validation`
  核心变化：OpenAI 的 Jalapeño 工程样品已在实验室以 production target frequency/power 运行 ML workloads，其中包括 `GPT-5.3-Codex-Spark`。
  为什么重要：这不是新模型发布，但说明 Codex/Coding 模型已经被纳入 OpenAI 自研推理芯片验证 workload，模型可用性和成本会受硬件栈影响。
  建议动作：模型追踪里将 `serving substrate` 与 `model capability` 分开，不把硬件发布误记为模型代际，但保留其对 Codex 模型可用性的影响。
  来源日期：`2026-06-24`
  来源：https://openai.com/index/openai-broadcom-jalapeno-inference-chip/

- 模型：`Gemini Interactions API model interface`
  公司：`Google`
  定位：`model/agent primary API surface`
  核心变化：Google 将 Interactions API 设为 Gemini models and agents 的 primary API，后续 long-running models 和 agents 的 frontier capabilities 预计更多落在这个接口；API 支持 model ID、agent ID、background execution、Managed Agents 和 tool combinations。
  为什么重要：模型能力的可用性开始由 API interface 决定；同一 Gemini 模型在 legacy generateContent 和 Interactions API 上可能获得不同 long-running / agent 能力。
  建议动作：Google 模型选型时记录 `API surface`：legacy generateContent、Interactions API、Managed Agents 分开标注。
  来源日期：`2026-06-25`
  来源：https://blog.google/innovation-and-ai/technology/developers-tools/interactions-api-general-availability/

### 状态变化

- OpenAI 最新模型状态：
  之前判断：GPT-5.5 是主力，GPT-5.5-Cyber 属于受控垂直模型线。
  当前判断：`GPT-5.5-Cyber full version` 已通过 trusted access 继续开放给防御者；同时 Jalapeño 显示 Codex 相关 workload 已进入自研推理硬件验证。
  变化原因：Daybreak 与 Jalapeño 同周发布，分别更新了垂直安全模型和模型 serving substrate。

- Anthropic 最新模型状态：
  之前判断：可用稳定基石模型退回至 `Claude Opus 4.8`。
  当前判断：判断保持不变，但 Opus 4.8 的重要新增面是 Claude Tag 的团队协作 runtime。
  变化原因：Claude Tag 页面明确其 works with Opus 4.8。

- Google 最新模型状态：
  之前判断：Gemini 3.5 Flash 与 Pro 继续渗透业务入口。
  当前判断：`Gemini 3.5 Flash` 新增内建 computer-use 能力，且 Gemini 模型/agents 的新能力正在向 Interactions API 主接口集中。
  变化原因：Google 本周发布 computer use in Gemini 3.5 Flash，并宣布 Interactions API GA。